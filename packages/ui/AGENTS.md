# UI package instructions

Own reusable accessible controls, tokens, and primitives only. Do not import application state, City domain behavior, Three.js, or asset metadata. Components need visible focus, keyboard operation, semantic names, and sufficient contrast. Preserve color-plus-pattern zone conventions. Update `UX_SPEC.md` and `ACCESSIBILITY.md` for behavioral changes.
